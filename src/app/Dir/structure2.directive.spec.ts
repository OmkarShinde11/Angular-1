import { Structure2Directive } from './structure2.directive';

describe('Structure2Directive', () => {
  it('should create an instance', () => {
    const directive = new Structure2Directive();
    expect(directive).toBeTruthy();
  });
});
