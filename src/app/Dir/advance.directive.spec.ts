import { AdvanceDirective } from './advance.directive';

describe('AdvanceDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvanceDirective();
    expect(directive).toBeTruthy();
  });
});
