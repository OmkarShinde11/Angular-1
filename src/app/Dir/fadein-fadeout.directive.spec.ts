import { FadeinFadeoutDirective } from './fadein-fadeout.directive';

describe('FadeinFadeoutDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeinFadeoutDirective();
    expect(directive).toBeTruthy();
  });
});
